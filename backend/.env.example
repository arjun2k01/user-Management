# ============================================
# User Management System - Environment Variables
# ============================================

# ============== APPLICATION ==============
NODE_ENV=development
PORT=5000

# ============== DATABASE ==============
# MongoDB connection URI
# Local: mongodb://localhost:27017/user-management
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/user-management
MONGO_URI=mongodb://localhost:27017/user-management

# ============== JWT AUTHENTICATION ==============
# Generate a strong 64-character secret key
# Example: openssl rand -hex 32
# CRITICAL: Change this in production to a strong random string
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-to-a-64-char-string

# JWT Token expiration time
JWT_EXPIRE=7d

# ============== FRONTEND ==============
# Frontend URL for CORS configuration
CLIENT_URL=http://localhost:5173

# ============== EMAIL CONFIGURATION ==============
# Configure for password reset emails
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@usermanagement.com

# ============== LOGGING ==============
# Log level: error, warn, info, debug
LOG_LEVEL=info

# ============== RATE LIMITING ==============
# Rate limit window (in minutes)
RATE_LIMIT_WINDOW=15

# Rate limit max requests
RATE_LIMIT_MAX_REQUESTS=100

# Auth endpoint rate limit (stricter)
AUTH_RATE_LIMIT_MAX=5

# ============== API KEYS (if using external services) ==============
# Add external API keys here
# SENDGRID_API_KEY=your-sendgrid-key
# STRIPE_SECRET_KEY=your-stripe-key

# ============== SECURITY ==============
# Enable HTTPS in production
USE_HTTPS=false

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

